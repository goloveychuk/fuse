{
  "packageManager": "yarn@4.9.1",
  "scripts": {
    "test": "yarn workspaces foreach --all --parallel --topological-dev --interlaced --verbose run test"
  },
  "workspaces": {
    "packages": [
      "package",
      "otherpackage"
    ]
  }
}
